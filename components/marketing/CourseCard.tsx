import Link from 'next/link';
export default function CourseCard({course}:{course:any}){return <article className='rounded-2xl bg-white p-5 shadow-premium transition hover:-translate-y-1'><div className='mb-3 text-xs text-primary'>{course.badge}</div><h3 className='font-semibold'>{course.title}</h3><p className='text-sm text-slate-500'>{course.instructor}</p><p className='mt-2 text-sm'>⭐ {course.rating} • {course.enrolled} enrolled • {course.duration}</p><div className='mt-3 flex items-center gap-2'><span className='font-bold text-primary'>৳{course.price}</span><span className='text-sm line-through text-slate-400'>৳{course.oldPrice}</span></div><div className='mt-4 flex gap-2'><Link className='rounded-xl border px-3 py-2 text-sm' href={`/courses/${course.slug}`}>Preview</Link><button className='rounded-xl bg-primary px-3 py-2 text-sm text-white'>Enroll</button></div></article>}
